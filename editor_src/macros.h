//Screen dimension constants
const int EDITOR_SCREEN_WIDTH = 1360;
const int EDITOR_SCREEN_HEIGHT = 768;

const int FONT_SIZE = 26;

const int MIN_MAP_SIZE = 17; // multiplicado por el tile_size tiene que dar el tamaño de la pantalla
const int MAX_MAP_SIZE = 30;
const int MIN_SIZE = 1; // for de bomb and spawn sites
const int MAX_SIZE = 5;

const int TILE_SIZE = 80;